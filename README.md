# no_zan_lane

NoZanLane は Flutter で作成したタスクプランナーです。

## Flutter Version

- FVM で Flutter `3.41.1` を使用します。

## Setup

```bash
fvm install 3.41.1
fvm use 3.41.1 --force
fvm flutter pub get
```

## Run

```bash
fvm flutter run -d chrome
```
